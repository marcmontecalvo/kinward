"""Built-in assistant visual-identity packs (Epic 3 Story 3.7).

Each ``<id>.json`` file in this package is one catalog entry, loaded by
``kinward.application.visual_packs``. Adding a pack is authoring a new JSON file
here (see ``scripts/new_visual_pack.py``) - never a code change to the catalog
engine or to any renderer that consumes it.
"""
